apiVersion: v1
kind: Service
metadata:
  name: grades-svc
  namespace: grades
spec:
  type: LoadBalancer
  selector:
    app: grades-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP